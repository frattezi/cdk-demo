import * as asset_schema from '@aws-cdk/cdk-assets-schema';
import * as cdk_assets from 'cdk-assets';
export declare class AssetManifestBuilder {
    private readonly manifest;
    addFileAsset(id: string, source: asset_schema.FileSource, destination: asset_schema.FileDestination): void;
    addDockerImageAsset(id: string, source: asset_schema.DockerImageSource, destination: asset_schema.DockerImageDestination): void;
    toManifest(directory: string): cdk_assets.AssetManifest;
}
